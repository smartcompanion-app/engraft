## What changed

<!-- What does this do, and why? Link any issue it closes. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] A change to how an action behaves comes with an e2e fixture under `tests/e2e/fixtures/`
- [ ] Documentation under `docs/` reflects the change
- [ ] A changeset is included (`npm run changeset`), unless this changes nothing that gets published
