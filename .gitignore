node_modules/
dist/
coverage/
*.tsbuildinfo

# engraft's own staging directory, created and removed by `engraft apply`.
# Tracked here in case a crashed run leaves one behind.
.engraft/

.DS_Store

# npm pack output
*.tgz
