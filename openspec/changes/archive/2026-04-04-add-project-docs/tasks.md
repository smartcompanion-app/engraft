## 1. README.md

- [x] 1.1 Create README.md with project description and motivation section (what engraft does, comparison with templating/forking/manual editing)
- [x] 1.2 Add installation instructions (`pip install engraft`)
- [x] 1.3 Add quick-start example with minimal template file, values file, and `engraft apply` command
- [x] 1.4 Add action reference section documenting all four action types (`json_replace`, `html_replace`, `regex_replace`, `file_replace`) with YAML snippets
- [x] 1.5 Add development section (install dev deps, run tests, run linter)

## 2. CLAUDE.md

- [x] 2.1 Create CLAUDE.md with project overview and core concept (two-file model)
- [x] 2.2 Add architecture description (CLI → engine → models → actions registry with `@register` decorator)
- [x] 2.3 Add development commands (install, test, lint, CLI entry point)
- [x] 2.4 Add coding conventions (Python 3.10+, type hints, ruff rules, pytest)
