variables:
  flag:
    description: Flag value — written as 'yes' in values.yaml. Under YAML 1.2 that is the string 'yes', not the boolean true.

customizations:
  - action: json_replace
    file: config.json
    replace:
      - selector: $.flag
        variable: flag
